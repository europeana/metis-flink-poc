kind: NetworkPolicy
apiVersion: networking.k8s.io/v1
metadata:
  name: mcs-access-from-flink-poc
  namespace: ecloud-dev-test
spec:
  podSelector:
    matchLabels:
      app: mcs-app
  ingress:
    - ports:
        - protocol: TCP
          port: 8080
      from:
        - namespaceSelector:
            matchLabels:
              name: ecloud-flink-poc
  policyTypes:
    - Ingress
