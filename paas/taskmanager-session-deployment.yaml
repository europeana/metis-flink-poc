apiVersion: apps/v1
kind: Deployment
metadata:
  name: flink-taskmanager
spec:
  replicas: 2
  selector:
    matchLabels:
      app: flink
      component: taskmanager
  template:
    metadata:
      labels:
        app: flink
        component: taskmanager
    spec:
      initContainers:
        - name: copy-conf
          image: registry.paas.psnc.pl/base/busybox:1.28
          command: [ "/bin/sh","-c","cp /conf-copy/* /opt/flink/conf;" ]
          volumeMounts:
            - name: flink-config-volume
              mountPath: /conf-copy
            - name: conf-volume
              mountPath: /opt/flink/conf
      containers:
        - name: taskmanager
          image: apache/flink:1.18.1-scala_2.12
          args: ["taskmanager"]
          ports:
            - containerPort: 6122
              name: rpc
          livenessProbe:
            tcpSocket:
              port: 6122
            initialDelaySeconds: 30
            periodSeconds: 60
          volumeMounts:
            - name: flink-config-volume
              mountPath: /conf-copy
            - name: conf-volume
              mountPath: /opt/flink/conf
            - name: log-volume
              mountPath: /opt/flink/log
      volumes:
        - name: flink-config-volume
          configMap:
            name: flink-config
            defaultMode: 0777
            items:
              - key: flink-conf.yaml
                path: flink-conf.yaml
              - key: log4j-console.properties
                path: log4j-console.properties
        - emptyDir: { }
          name: log-volume
        - emptyDir: { }
          name: conf-volume